export function Home(props) {
    return(
        <div>
    <h1>Welcome {props.name}</h1>
    <h3>Please use the links above</h3>
    <h3>Or type your name in the url above after welcome/yournamehere</h3>
    </div>
    )
}